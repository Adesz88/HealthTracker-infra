# HealthTracker-infra
docker volume create health-tracker-jenkins

Configure Docker to use without sudo and in the Dockerfile-jenkins change the docker group number to the number from this 'getent group docker'